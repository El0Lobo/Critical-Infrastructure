# Terminal Logger (local)

Logs all VS Code terminal output to `terminal.log` in the workspace root.

## Run in VS Code

1. Open this folder in VS Code: `vscode-terminal-logger`
2. Press `F5` to launch an Extension Development Host.
3. Terminal output in the dev host will append to `terminal.log`.

Use the command palette: `Terminal Logger: Open Log`.
